# Copy format for Streamlit Cloud → Settings → Secrets
# Use exact key name GROQ_API_KEY (root level, not nested)

GROQ_API_KEY = "gsk_your_groq_api_key_here"

# Do NOT set this on production (forces demo mode):
# USE_MOCK_LLM = "1"
