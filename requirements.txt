# ─────────────────────────────────────────────
# AI Interview Agent — Python Dependencies
# ─────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Frontend
streamlit>=1.35.0

# LLM (Groq / Llama)
groq>=0.9.0
python-dotenv>=1.0.0

# OpenAI (Whisper STT — optional)
openai>=1.30.0

# Text-to-Speech
gTTS>=2.5.1

# Audio (optional — install locally for mic capture: pip install sounddevice soundfile pydub)
# pydub>=0.25.1
# sounddevice>=0.4.6
# soundfile>=0.12.1
# numpy>=1.26.0

# HTTP client (used by Streamlit frontend to call FastAPI)
requests>=2.31.0

# Utilities
python-multipart>=0.0.9   # for FastAPI file uploads
pydantic>=2.7.0

# Note: sqlite3 is part of Python standard library — no install needed.
